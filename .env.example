# OpenRouter (required for ideation)
OPENROUTER_API_KEY=sk-or-v1-...

# Callback URL for implementation modules (must be reachable from Modal; use ngrok for local dev)
# CALLBACK_BASE_URL=http://localhost:3001

# Optional: GitHub + Vercel for real deploy pipeline
# GITHUB_TOKEN=ghp_...
# VERCEL_TOKEN=...

# Optional: Modal implementation (if set, spawns Modal jobs instead of mock)
# MODAL_IMPLEMENTATION_URL=https://...--epoch-implementation-trigger.modal.run

# Evaluator webhook: POST [{ url, pitch }, ...] when all jobs complete (one entry per job)
# EVALUATOR_WEBHOOK_URL=https://your-evaluator.com/webhook
# Legacy: WEBHOOK_URL is used as EVALUATOR_WEBHOOK_URL if EVALUATOR_WEBHOOK_URL is not set
# WEBHOOK_URL=https://your-server.com/webhook

# Server port (default 3001)
# PORT=3001
